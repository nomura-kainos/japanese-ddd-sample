@startuml
!include color.inc

package 実装 {
    actor 実装者
    (PRを作る)
note right
    タイトル: WIP タスク名
    ラベル: h1〜h8の中から選ぶ
    本文: 実装内容を記入
end note
実装者->(PRを作る)
(PRを作る)-->(実装中)
(実装中)-->(実装完了)
note right
    適切な項目にチェックを入れる。
    タイトルを「(レビュー待ち) タスク名」にする。
    reviewersにレビュー者を選択する。
end note
}

package レビュー {
(実装完了)-->(レビューする)
レビューワー->(レビューする)
(レビューする)->(フィードバックする)
note right
    タイトル: (フィードバック) タスク名
end note

(フィードバックする)->(実装中)
(レビューする)-->(開発ブランチへマージする)
}
@enduml
